## /etc/config/luci_plugins — both sections, as on the device. My earlier file
## had only `config plugins 'plugins'` and then only `config global 'global'`;
## the device carries both.
config plugins 'plugins'
	option enabled '1'

config global 'global'
	option enabled '1'
